# Requirements: <slug>

## Background

<!-- Why this work exists. The problem or the request behind it, in a few
     sentences. Link to the issue, conversation, or incident if there is one. -->

## Goal

<!-- What is true once this is done, stated as an outcome rather than a task
     list. One or two sentences. -->

## Acceptance criteria

<!-- Verifiable statements in "when X, then Y" form. Each one must be
     checkable by someone who did not write the code. Avoid "works
     correctly" and "is fast"; state the observable condition. -->

- When <condition>, then <observable result>.
- When <condition>, then <observable result>.

## Out of scope

<!-- What this work deliberately does not cover, so review does not treat
     the absence as a defect. -->

-

## Open questions

<!-- Unresolved points, each marked so they are easy to find. Requirements
     cannot be approved while any [OPEN:] remains. -->

- [OPEN: <question>]
